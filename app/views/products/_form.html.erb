<%= simple_form_for @product, html:{multipart: true} do |f| %>
  <% if @product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product.errors.count, "error") %> prohibited this product from being saved:</h2>

      <ul>
      <% @product.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<%#<div class="field">  <%#  instead of this 4 line if we use simple_form_for den we write only this  "<%=f.input :image percnt >" thats it and it wll work, and for submit function "f.button :submit" -%>
 <%#<%=f.label :image %>
 <%#<%=f.file_field :image %>
<%#</div>

 <%#<div class="field">
   <%= f.label :name %> 
  <%#<%= f.text_field :name %>
  <%#<%= f.input  :name %> 

<%#  <div class="field">
    <%= f.label :description %>
   <%# <%= f.text_field :description %>
  <%#</div>
  <div class="actions">
    <%= f.submit %>
  <%#</div>
<% end %> 

<br><%= f.input :image, :as=> :file %> <br>
<%= f.input :name %> <br>
<%= f.input :description %> <br>
<%= f.button :submit %>

<% end %>